<%
parameters = {
    title: "ホーム",
    entitle: "Home",
    css: "index",
    js: "app.min",
    description: config.commons.description,
    ogpimage: config.commons.url + parameters.ogpimage,
    originyear: 1885
}
articleUrl = config.commons.url
%><%- include("util/_init_load") %><%- include("util/_headset", { config, parameters }) %>
<!DOCTYPE html>
<html lang="ja">
<head>
<%- include("partial/_head", { config, parameters, commonVar, metaTitle, metaDescription, metaOgpimage }) %>
</head>
<body data-spy="scroll" data-target="#navbarMenu" data-offset="<%= navbarHeight %>" class="<%= id %>" id="<%= id %>">
<div id="wrapper">
<div class="upper">
<%- include("partial/_header", { config, commonVar }) %>
    <!-- main -->
    <main class="main <%= id %>main">
        <section class="container my-5 year_form" id="year_form">
            <form class="needs-validation" novalidate>
                <div class="form-group row">
                    <label for="year" class="col-md-2 col-form-label text-right">年(西暦)</label>
                    <input type="number" class="form-control col-md-10" id="year" min="<%= parameters.originyear %>" required>
                    <div class="offset-md-2 col-md-10">
                        <div class="invalid-feedback" id="invalid_message"></div>
                    </div>
                </div>
                <div class="form-group row">
                        <div class="offset-md-2 col-md-10">
                            <button class="btn btn_submit" id="submit_button" type="submit"><i class="fas fa-fw fa-retweet mr-1" aria-hidden="true"></i>変換</button>
                        </div>
                    </div>
                <div class="form-group row">
                    <div class="offset-md-2 col-md-10">
                        <i class="fas fa-fw fa-arrow-down fa-2x" aria-hidden="true"></i>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="gensokyo_ki" class="col-md-2 col-form-label text-right">季</label>
                    <output type="text" class="form-control col-md-10" id="gensokyo_ki"></output>
                </div>
                <div class="form-group row">
                    <label for="gensokyo_nen" class="col-md-2 col-form-label text-right">年</label>
                    <output type="text" class="form-control col-md-10" id="gensokyo_nen"></output>
                    <div class="offset-md-2 col-md-10">
                        <div class="invalid-feedback" id="accidentAsylum"><i class="fas fa-fw fa-exclamation-triangle mr-1" aria-hidden="true"></i>大結界異変の年です！</div>
                    </div>
                </div>
                <span class="d-none" data-zero="<%= parameters.originyear %>" id="origin_year"></span>
              </form>
        </section>
        <section class="container my-5 about" id="about">
            <h2 class="mt-3 mb-2"><i class="fas fa-fw fa-question mr-1" aria-hidden="true"></i>このページについて<small class="ml-1">About this page</small></h2>
            <div class="about_contents">
                <ul>
                    <li>このページは、西暦から幻想郷の紀年法(「季(第○季)」と「年(▲と■と★の年)」)を算出するプログラムのページです。</li>
                    <li>「年(西暦)」に西暦の年を入力し、「変換」ボタンをクリックすると計算を行います。</li>
                    <li><%= parameters.originyear %>年(明治18年)より前は計算できません</li>
                </ul>
                <h3 class="mt-3 mb-2">参考</h3>
                <ul>
                    <li><a href="http://thwiki.info/?%B8%B8%C1%DB%B6%BF%C7%AF%C9%BD">東方Wiki - 幻想郷年表</a></li>
                </ul>
            </div>
        </section>
        <section class="container my-5 share" id="share">
                <h2 class="mt-3 mb-2"><i class="fas fa-fw fa-share-alt mr-1" aria-hidden="true"></i>シェア<small class="ml-1">SNSShare</small></h2>
                <div class="share_contents">
<%- include("partial/_article_snsbutton", { config, articleUrl, metaTitle }) %>
                </div>
            </section>
    </main>
    <!-- /main -->
</div>
<%- include("partial/_footer", { config, copyRightYear }) %>
</div>
<%- include("partial/_js_loader", { parameters, relPath }) %>
</body>
</html>